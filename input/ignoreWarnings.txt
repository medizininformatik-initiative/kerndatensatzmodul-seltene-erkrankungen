== Suppressed Messages ==

# Add warning and/or information messages here after you've confirmed that they aren't really a problem
# (And include comments like this justifying why)
# See https://github.com/FHIR/sample-ig/blob/master/input/ignoreWarnings.txt for examples
#
# NOTE: ignoreWarnings.txt only suppresses WARNINGS and HINTS — it cannot suppress
# ERRORS. Terminology-server false-positives that surface as errors (e.g. the
# BCP-47/all-languages "UNABLE_TO_INFER_CODESYSTEM" / "Terminology_TX_NoValid_16"
# messages on inherited translation metadata) are reclassified via the advisory
# framework in ../advisor.json (passed to the IG Publisher with -advisor-file),
# NOT here.
